{% extends "_base.html.j2" %}
{% block title %}Momo Paper · Stats Report · {{ meta.title }}{% endblock %}
{% block extra_css %}
  <style>
    .card-surface {
      background: var(--paper);
      border: 1px solid var(--line);
      box-shadow: var(--shadow-soft);
    }
    .card-surface:hover {
      border-color: var(--brand);
      box-shadow: var(--shadow-surface);
    }
    .chart-surface {
      background: var(--paper);
      border: 1px solid var(--line);
      border-radius: var(--radius);
      box-shadow: var(--shadow-soft);
    }
  </style>
{% endblock %}
{% block content %}
  <header class="mb-[var(--space-3xl)] text-left">
    <p class="block mb-[var(--space-sm)] font-mono text-[0.75rem] font-medium uppercase tracking-[0.08em] text-brand">{{ meta.eyebrow | default('Momo Paper / stats_report / ' + locale) }}</p>
    <h1>{{ meta.title }}</h1>
    <p class="max-w-full text-[1.1rem] text-subtle md:max-w-[var(--max-width-text)]">{{ meta.subtitle }}</p>
  </header>

  {% if sections.kpis %}
  <section>
    <h2>{% if locale == 'zh-CN' %}KPI{% else %}KPIs{% endif %}</h2>
    <div class="mt-[var(--space-xl)] grid grid-cols-[repeat(auto-fit,minmax(160px,1fr))] gap-[var(--space-lg)]">
      {% for kpi in sections.kpis %}
      <div class="card-surface rounded-lg p-[var(--card-padding)] text-center transition-all duration-200 hover:-translate-y-0.5">
        <div class="text-[1.5rem] font-semibold leading-[1.2] text-ink">{{ kpi.value }}</div>
        <div class="mt-[var(--space-sm)] font-mono text-[12px] uppercase tracking-[0.04em] text-subtle">{{ kpi.label }}</div>
        {% if kpi.change %}<div class="mt-[var(--space-xs)] text-[14px] {% if kpi.trend == 'up' %}text-[var(--success)]{% elif kpi.trend == 'down' %}text-[var(--danger)]{% else %}text-subtle{% endif %}">{{ kpi.change }}</div>{% endif %}
      </div>
      {% endfor %}
    </div>
  </section>
  {% endif %}

  {% if sections.trends %}
  <section>
    <h2>{% if locale == 'zh-CN' %}趋势{% else %}Trends{% endif %}</h2>
    {% for para in sections.trends.body %}
    <p>{{ para }}</p>
    {% endfor %}
    {% if sections.trends.chart %}
    <div class="chart-surface my-[var(--space-xl)] p-[var(--space-lg)] [&_svg]:block [&_svg]:w-full">
      {{ sections.trends.chart | render_chart }}
    </div>
    {% endif %}
    {% if sections.trends.anomalies %}
    <div class="mt-[var(--space-xl)] rounded-[0_var(--radius)_var(--radius)_0] border-l-[3px] border-l-[var(--danger)] bg-[var(--accent-muted)] p-[var(--space-lg)_var(--space-xl)] text-[1rem] before:mb-[var(--space-sm)] before:block before:font-mono before:text-[0.65rem] before:font-semibold before:uppercase before:tracking-[0.08em] before:text-[var(--danger)] before:content-['Anomaly']">
      <ul class="!mt-0 list-none pl-0">
        {% for a in sections.trends.anomalies %}
        <li class="relative mb-[var(--space-sm)] pl-[var(--space-xl)] text-[0.95rem] leading-[1.6] text-[var(--danger)] before:absolute before:left-[4px] before:top-[10px] before:h-[6px] before:w-[6px] before:rounded-full before:bg-[var(--danger)] before:content-['']">{{ a }}</li>
        {% endfor %}
      </ul>
    </div>
    {% endif %}
  </section>
  {% endif %}

  {% if sections.segments %}
  <section>
    <h2>{% if locale == 'zh-CN' %}分布与分群{% else %}Segments{% endif %}</h2>
    {% for seg in sections.segments %}
    <div class="mb-[var(--space-xl)]">
      <h4 class="mb-[var(--space-sm)] text-[1rem] font-semibold">{{ seg.name }}</h4>
      {% for para in seg.body %}
      <p>{{ para }}</p>
      {% endfor %}
      {% if seg.chart %}
      <div class="chart-surface mt-[var(--space-md)] p-[var(--space-lg)] [&_svg]:block [&_svg]:w-full">
        {{ seg.chart | render_chart }}
      </div>
      {% endif %}
    </div>
    {% endfor %}
  </section>
  {% endif %}

  {% if sections.diagrams %}
  <section>
    <h2>{% if locale == 'zh-CN' %}图示原语{% else %}Diagram Primitives{% endif %}</h2>
    {% for item in sections.diagrams %}
    <div class="mb-[var(--space-xl)]">
      <h4 class="mb-[var(--space-sm)] text-[1rem] font-semibold">{{ item.name }}</h4>
      {% for para in item.body %}
      <p>{{ para }}</p>
      {% endfor %}
      {% if item.diagram %}
      <div class="card-surface rounded-[var(--radius)] p-[var(--space-2xl)]">
        {{ item.diagram | render_diagram }}
      </div>
      {% endif %}
    </div>
    {% endfor %}
  </section>
  {% endif %}

  {% if sections.method %}
  <section>
    <h2>{% if locale == 'zh-CN' %}方法说明{% else %}Methodology{% endif %}</h2>
    {% for para in sections.method.body %}
    <p>{{ para }}</p>
    {% endfor %}
  </section>
  {% endif %}
{% endblock %}
{% block footer %}
  <div class="mt-[var(--space-2xl)] flex flex-wrap justify-between gap-[var(--space-sm)] border-t border-[var(--border-light)] pt-[var(--space-lg)] font-mono text-[0.75rem] text-[var(--text-muted)] max-md:flex-col">
    <span>{{ meta.period | default('') }}</span>
    <span></span>
  </div>
{% endblock %}
